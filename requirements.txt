# Video Subtitle Remover Pro - Dependencies
# ==========================================

# Core
# opencv-python >= 4.12.0 patches CVE-2025-53644 (uninitialised pointer in
# JPEG read can become an arbitrary heap write on a crafted file).
# Pillow >= 11.3.0 patches CVE-2026-25990 (PSD loader out-of-bounds write).
numpy>=1.21.0
opencv-python>=4.12.0
Pillow>=11.3.0

# AI/ML Frameworks (choose based on your GPU)
# ------------------------------------------
# IMPORTANT: torch >= 2.10.0 is required.
# CVE-2026-24747 / CVE-2025-32434: torch.load weights_only RCE in PyTorch
# 2.9.1 and earlier. simple-lama-inpainting loads weights via torch.load,
# so this is a runtime concern, not just a transitive-dependency one.

# For NVIDIA GPUs (CUDA 11.8):
# pip install torch>=2.10.0 torchvision>=0.25.0 --index-url https://download.pytorch.org/whl/cu118
# pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/

# For AMD/Intel (DirectML):
# pip install torch-directml==0.2.5.dev240914
# pip install paddlepaddle==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

# For CPU only:
# pip install torch>=2.10.0 torchvision>=0.25.0 --index-url https://download.pytorch.org/whl/cpu
# pip install paddlepaddle==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cpu/

# Text Detection (install at least one, listed in priority order)
# RapidOCR runs PP-OCR via ONNX Runtime -- 4-5x faster than the upstream
# paddlepaddle build and free of its long-standing memory-leak issues.
rapidocr>=2.0.0; python_version>="3.10"
rapidocr-onnxruntime>=1.4.0; python_version<"3.10"
paddleocr>=3.0.0
# surya-ocr>=0.9.0  # Optional: fast layout-aware detection (GPL -- see license)
easyocr>=1.7.0

# Real AI Inpainting (LAMA mode)
simple-lama-inpainting>=0.1.0

# GUI Enhancement (optional)
# tkinterdnd2>=0.3.0  # For native drag-drop support

# Dynamic Watermark Mode (header button + tool/dynamic_inpaint_cli.py)
# --------------------------------------------------------------------
# The experimental moving-watermark pipeline shells out to a sibling
# watermark_remover project's bundled conda env -- ProPainter's heavy
# deps (transformers, groundingdino's CUDA-compiled ops, av, einops,
# scikit-image, timm) intentionally are NOT pulled into this venv.
#
# Setup steps:
#   1. Clone a watermark_remover checkout next to this repo
#      (or anywhere accessible) and run its installer so env/python.exe
#      and ckpt/R50_DeAOTL_PRE_YTB_DAV.pth exist.
#   2. Either place it at ../watermark_remover, or set
#      VSR_WATERMARK_REMOVER_PATH to its absolute path.
#   3. Verify with:  python -m tool.check_dynamic_mode
#   4. Launch VSR Pro and click "Watermark Mode" in the header.
